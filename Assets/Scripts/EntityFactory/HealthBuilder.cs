using UnityEngine;
/*
public class HealthBuilder : IEntityComponentPlugin
{
    public void Apply(GameObject entity, BaseEntityData data)
    {
        if(data.Health == null) return;

        var health = entity.AddComponent<HealthSystem>();
        health.Initialize(data.Health.Max, data.Health.RegenRate);
    }
}
*/