using System;

public class TargetingBuilderModule : IContextBuilderModule
{
    public void Build(BtContext context)
    {
        /*
        var scriptName = nameof(TargetingContextBuilderModule);
        var agent = context.Agent;
        var blackboard = context.Blackboard;
        
        // 1. Fail-fast if required profile dictionary is missing
        if (blackboard.TargetingProfiles == null)
            throw new Exception($"[{scriptName}] TargetingProfiles not injected into blackboard. Check profile loader or registration.");

        var config = 
        
        // Try to find any IMovementNode component (generic, could be NavMeshMoveToTarget or another)

        */
    }
}